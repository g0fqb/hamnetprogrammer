<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="HamNetProgrammer.Desktop.Views.ZonesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#10161d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12" Margin="0,0,0,12">
            <TextBlock Text="Zones" FontSize="22" FontWeight="Bold" Foreground="#e0924a" VerticalAlignment="Center" />
            <TextBlock x:Name="SummaryText" FontSize="12" Foreground="#8896a0" VerticalAlignment="Center" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#1a232c" CornerRadius="6">
                    <ListView x:Name="ZoneListView" SelectionChanged="OnZoneSelectionChanged" Padding="4">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontFamily="Consolas" Padding="8,6" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="6" Margin="0,8,0,0">
                    <Button x:Name="NewZoneButton" Content="New" Click="OnNewZoneClicked" ToolTipService.ToolTip="New zone" />
                    <Button x:Name="RenameZoneButton" Content="Rename" Click="OnRenameZoneClicked" ToolTipService.ToolTip="Rename selected zone" />
                    <Button x:Name="DeleteZoneButton" Content="Delete" Click="OnDeleteZoneClicked" ToolTipService.ToolTip="Delete selected zone" />
                </StackPanel>
            </Grid>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
                    <Button x:Name="NewChannelButton" Content="New Channel..." Click="OnNewChannelClicked"
                            ToolTipService.ToolTip="Create a new channel from a talkgroup, using this zone's frequency/colour-code/slot." />
                    <Button x:Name="AddChannelButton" Content="Add Existing Channel..." Click="OnAddChannelClicked"
                            ToolTipService.ToolTip="Add a channel that already exists elsewhere but isn't in this zone yet." />
                    <TextBlock x:Name="ChannelHintText" FontSize="11" Foreground="#8896a0" VerticalAlignment="Center"
                               Text="Select a zone, then add/reorder/remove its channels." />
                </StackPanel>

                <Border Grid.Row="1" Background="#1a232c" CornerRadius="6">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Background="#212c36" Padding="10,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40" />
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="90" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="190" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="#" FontSize="11" Foreground="#8896a0" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" Text="Name" FontSize="11" Foreground="#8896a0" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="2" Text="MHz" FontSize="11" Foreground="#8896a0" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="3" Text="Talkgroup" FontSize="11" Foreground="#8896a0" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="4" Text="CC/TS" FontSize="11" Foreground="#8896a0" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="5" Text="" FontSize="11" Foreground="#8896a0" FontWeight="SemiBold" />
                        </Grid>

                        <ListView x:Name="ChannelListView" Grid.Row="1" Padding="4" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="10,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="160" />
                                            <ColumnDefinition Width="90" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="60" />
                                            <ColumnDefinition Width="190" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Position}" FontFamily="Consolas" FontSize="12" Foreground="#8896a0" />
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12" Foreground="#dce6ea" />
                                        <TextBlock Grid.Column="2" Text="{Binding FrequencyMHz}" FontFamily="Consolas" FontSize="12" Foreground="#dce6ea" />
                                        <TextBlock Grid.Column="3" Text="{Binding TalkGroup}" FontSize="12" Foreground="#dce6ea" />
                                        <TextBlock Grid.Column="4" Text="{Binding ColorCodeSlot}" FontFamily="Consolas" FontSize="12" Foreground="#8896a0" />
                                        <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="4">
                                            <Button Content="Edit" FontSize="10" Padding="6,2" Tag="{Binding ChannelId}" Click="OnEditChannelClicked" />
                                            <Button Content="Up" FontSize="10" Padding="6,2" Tag="{Binding Position}" Click="OnMoveUpClicked" />
                                            <Button Content="Down" FontSize="10" Padding="6,2" Tag="{Binding Position}" Click="OnMoveDownClicked" />
                                            <Button Content="Remove" FontSize="10" Padding="6,2" Tag="{Binding Position}" Click="OnRemoveChannelClicked" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
